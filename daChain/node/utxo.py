from dataclasses import dataclass

from daChain.core.da_types import Tx, TxIn, TxOut










def _is_output_valid(prev_TxOut: TxOut) -> bool:
    

    return


def _is_input_output_stakes_sum_valid(Tx: Tx) -> bool:
    stakes = 0
    
    for input in Tx.inputs:
        get_
        return
    
    
    return
        



def verify_tx(tx: Tx) -> bool:


    return False